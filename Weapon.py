class weapon():
    #some variables
    wpntype = None #0 if melee, 1 else
    ammo = None #-1 if melee, 0 or more else
    damage = None
    sprite = None
    atk_distance = None #1 or more if melee, -1 else
    
    description = "There must be <item> description. But now there is some sample text. :c"
    
    def usage():
        pass